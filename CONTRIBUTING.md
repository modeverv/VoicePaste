# CONTRIBUTING.md — VoicePaste コントリビューションガイド

VoicePaste へのコントリビューションを歓迎します。バグ報告・機能提案・コードの改善、いずれも大歓迎です。

---

## はじめる前に

- `SPEC.md` でプロジェクトの設計原則と仕様を確認してください
- `AGENTS.md` で作業規約（テスト・コミット等）を確認してください
- 大きな変更を加える前に、まず Issue で議論してください

---

## 開発環境のセットアップ

```bash
git clone https://github.com/yourname/voicepaste
cd voicepaste
make install
make test   # 全テストがパスすることを確認する
```

---

## ブランチ戦略

| ブランチ | 用途 |
|----------|------|
| `main` | リリース済み安定版 |
| `develop` | 開発中の統合ブランチ |
| `feature/<名前>` | 機能追加 |
| `fix/<名前>` | バグ修正 |

作業は必ず `develop` から分岐して、`develop` に向けてPRを出してください。

```bash
git checkout develop
git checkout -b feature/your-feature-name
```

---

## コミットメッセージ規約

[Conventional Commits](https://www.conventionalcommits.org/) に従います。

```
<type>: <概要>

[本文（任意）]
```

### type の種類

| type | 用途 |
|------|------|
| `feat` | 新機能 |
| `fix` | バグ修正 |
| `docs` | ドキュメントのみの変更 |
| `test` | テストの追加・修正 |
| `refactor` | 機能変更を伴わないリファクタリング |
| `chore` | ビルド・依存関係等の雑務 |

### 例

```
feat: チャンク処理スレッドを追加する

録音中の音声を chunk_seconds ごとに分割し、
Whisper に流してリアルタイム表示する。
```

```
fix: macOS で pbcopy がエンコードエラーになる問題を修正する
```

---

## Pull Request のガイドライン

### PR を出す前に確認すること

```bash
make lint   # リントエラーがないこと
make test   # 全テストがパスすること
```

カバレッジが80%を下回る変更は受け付けません。

### PR の説明に含めること

- 変更の目的・背景
- 変更内容のサマリー
- 動作確認した環境（OS・Pythonバージョン）
- 関連するIssue番号（あれば）

### PR のサイズ

1つのPRで変更するスコープを小さく保ってください。大きな機能追加は複数のPRに分割することを推奨します。

---

## Issue の書き方

### バグ報告

以下の情報を含めてください。

- OS・Pythonバージョン・Whisperバックエンド
- 再現手順
- 期待する動作
- 実際の動作
- エラーログ（あれば）

### 機能提案

- 解決したい問題・動機
- 提案する解決策
- 代替案（検討した場合）

---

## 対応していないコントリビューション

以下は現時点でスコープ外です。Issueで議論なしに実装してもマージできません。

- GUIフロントエンドの追加
- クラウドSTTバックエンドの追加（ローカルファーストの原則に反する）
- LLMによる後処理のコア組み込み（将来の拡張として設計中）
- 既存の `config.yaml` キーの破壊的変更

---

## ライセンス

コントリビューションはMITライセンスのもとで提供されます。
